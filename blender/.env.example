# Copy this file to ".env" (same folder) and fill it in.

# From Supabase → Settings → API
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-keep-this-secret

# Path to the Blender executable. If "blender" already works in your terminal,
# leave it as-is. Otherwise point to it, e.g. on Windows:
#   C:/Program Files/Blender Foundation/Blender 4.2/blender.exe
BLENDER_PATH=blender

# Full path to your .blend template file
KF_TEMPLATE=C:/KantoForge/kantoforge-case.blend

# The two Image datablock names in your .blend that hold the card and the
# background artwork (see render_job.py for how to find them)
KF_CARD_IMG_NAME=card_placeholder.png
KF_ART_IMG_NAME=art_placeholder.png

# How often to check for new jobs (seconds)
KF_POLL_SECONDS=5
